apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-app
  labels:
    app: web-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web-app
  template:
    metadata:
      labels:
        app: web-app
    spec:
      containers:
      - name: web-application
        image: dell123.jfrog.io/web-application/web-application:latest
        ports:
        - containerPort: 8080
      imagePullSecrets:
      - name: jfrogcred
